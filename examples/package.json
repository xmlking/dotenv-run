{
  "name": "platform",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "build:all": "nx run-many --target=build --all --parallel",
    "test:all": "nx run-many --target=test --all --parallel"
  },
  "devDependencies": {
    "@dotenv-run/cli": "^1.0.0",
    "@dotenv-run/load": "^1.0.0",
    "@dotenv-run/webpack": "^1.0.0",
    "@dotenv-run/rollup": "^1.0.0",
    "webpack": "^5.0.0",
    "@playwright/test": "^1.37.1",
    "webpack-cli": "^5.0.0",
    "rollup": "^3.0.0",
    "nx": "^16.5.5"
  }
}