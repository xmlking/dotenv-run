{
  "name": "dotenv-run",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel --no-cache --filter=@dotenv-run/*",
    "build": "turbo build  --filter=@dotenv-run/*"
  },
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.26.1",
    "turbo": "^1.10.12",
    "typescript": "^5.1.3"
  }
}